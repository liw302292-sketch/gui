upstream quote_frontend {
    server frontend:3000;
}

upstream quote_backend {
    server backend:8000;
}

server {
    listen 80;
    server_name _;

    client_max_body_size 25m;
    proxy_read_timeout 180s;
    proxy_send_timeout 180s;

    # ---------- 安全响应头 ----------
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options SAMEORIGIN always;
    add_header Referrer-Policy strict-origin-when-cross-origin always;

    # ---------- 后端 API ----------
    location /api/ {
        proxy_pass http://quote_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # ---------- 前端（官网 / 后台 / 公开报价页）----------
    location / {
        proxy_pass http://quote_frontend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # ---------- Next.js 静态资源 ----------
    location /_next/static/ {
        proxy_pass http://quote_frontend;
        proxy_cache_valid 200 30d;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
}

# =====================================================================
# 生产多域名示例（按需启用）
#
# server {
#     listen 80;
#     server_name www.example.com app.example.com quote.example.com;
#     location / { proxy_pass http://quote_frontend; ... }
# }
#
# server {
#     listen 80;
#     server_name api.example.com;
#     location / { proxy_pass http://quote_backend; ... }
# }
# =====================================================================

